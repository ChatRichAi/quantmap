# Quant EvoMap UI

基于 Next.js 的量化策略进化网络可视化界面，支持中英文切换。

## 功能特性

- 🌐 **国际化**: 完整的中英文支持，一键切换
- 📊 **力导向图可视化**: D3.js 驱动的基因进化网络
- 📈 **实时统计**: 基因数量、世代、存活率、夏普比率
- 🎨 **现代 UI**: Tailwind CSS 构建的深色主题界面

## 快速开始

```bash
# 安装依赖
cd quant-evomap-ui
npm install

# 启动开发服务器
npm run dev

# 访问
# 中文: http://localhost:3000/zh/evomap
# 英文: http://localhost:3000/en/evomap
```

## 项目结构

```
quant-evomap-ui/
├── app/[locale]/           # 国际化路由
│   ├── layout.tsx          # 主布局
│   ├── page.tsx            # 首页 (重定向到 evomap)
│   └── evomap/page.tsx     # EvoMap 可视化页面
├── components/
│   ├── layout/             # 布局组件
│   │   ├── Header.tsx
│   │   └── LanguageSwitch.tsx
│   └── visualization/      # 可视化组件
│       ├── ForceGraph.tsx
│       ├── GeneCard.tsx
│       ├── Legend.tsx
│       └── StatsBar.tsx
├── lib/
│   ├── api.ts              # API 客户端
│   └── utils.ts            # 工具函数
├── locales/
│   ├── en.json             # 英文翻译
│   └── zh.json             # 中文翻译
├── i18n.ts                 # 国际化配置
└── middleware.ts           # 路由中间件
```

## API 配置

默认会尝试连接 `localhost:8891` 或 `localhost:8889` 的后端 API。

如需修改，编辑 `lib/api.ts` 中的 `API_PORTS` 数组。

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **国际化**: next-intl
- **可视化**: D3.js v7
